** start of index.html **

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Product Landing Page — Acme Gadget</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <!-- Header with logo and navigation -->
  <header id="header">
    <div class="header-inner">
      <!-- logo image (must have an http(s) src) -->
      <a href="#top" class="logo-wrap">
        <img id="header-img" src="https://images.unsplash.com/photo-1523475496153-3d6cc2b4b1b8?w=400&q=80&auto=format&fit=crop" alt="Acme Gadget logo">
      </a>

      <!-- nav bar (inside header) -->
      <nav id="nav-bar" aria-label="Main navigation">
        <a class="nav-link" href="#features">Features</a>
        <a class="nav-link" href="#video-section">Video</a>
        <a class="nav-link" href="#pricing">Pricing</a>
        <a class="nav-link" href="#signup">Sign Up</a>
      </nav>
    </div>
  </header>

  <!-- top anchor -->
  <div id="top"></div>

  <!-- Hero / call to action -->
  <section class="hero">
    <div class="hero-inner">
      <div class="hero-copy">
        <h1>Acme Multitool — One Tool, Many Uses</h1>
        <p class="tag">The only gadget you'll need for home, travel, and adventure. Lightweight, powerful, and beautifully engineered.</p>
        <a class="btn" href="#signup">Preorder Now</a>
      </div>
      <div class="hero-image">
        <img src="https://images.unsplash.com/photo-1515879218367-8466d910aaa4?w=900&q=80&auto=format&fit=crop" alt="Acme gadget product photo">
      </div>
    </div>
  </section>

  <!-- Features -->
  <section id="features" class="section">
    <h2>Features</h2>
    <div class="features-grid">
      <div class="feature">
        <h3>Compact Design</h3>
        <p>Folds into your pocket yet powerful enough for daily tasks.</p>
      </div>
      <div class="feature">
        <h3>Multi-Tool</h3>
        <p>Includes screwdriver, knife, bottle opener, and more — all in one.</p>
      </div>
      <div class="feature">
        <h3>Durable Build</h3>
        <p>Aircraft-grade stainless steel and water-resistant seals.</p>
      </div>
    </div>
  </section>

  <!-- Video -->
  <section id="video-section" class="section">
    <h2>Watch the Demo</h2>
    <!-- required element: video with id="video" and src attribute -->
    <div class="video-wrap">
      <iframe id="video" src="https://www.youtube.com/embed/dQw4w9WgXcQ" title="Acme demo video" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    </div>
  </section>

  <!-- Pricing -->
  <section id="pricing" class="section">
    <h2>Pricing</h2>
    <div class="pricing-cards">
      <div class="card">
        <h3>Standard</h3>
        <p class="price">₹1,499</p>
        <p>All essential tools</p>
      </div>
      <div class="card featured">
        <h3>Pro</h3>
        <p class="price">₹2,999</p>
        <p>Includes premium case and warranty</p>
      </div>
      <div class="card">
        <h3>Bundle</h3>
        <p class="price">₹4,499</p>
        <p>Two tools + accessory pack</p>
      </div>
    </div>
  </section>

  <!-- Sign up / form -->
  <section id="signup" class="section">
    <h2>Preorder / Join Waitlist</h2>
    <!-- form required by tests -->
    <form id="form" action="https://www.freecodecamp.com/email-submit" method="POST">
      <!-- email input (required attributes) -->
      <label for="email" class="sr-only">Email address</label>
      <input id="email" name="email" type="email" placeholder="Enter your email address" required>
      <!-- submit input -->
      <input id="submit" type="submit" value="Submit">
    </form>
  </section>

  <footer id="footer" class="section small">
    <p>&copy; 2025 Acme Gadget Co. All rights reserved.</p>
  </footer>
</body>
</html>


** end of index.html **

** start of styles.css **

/* Basic reset */
* { box-sizing: border-box; margin: 0; padding: 0; }

:root{
  --bg: #f4f7fb;
  --card: #fff;
  --accent: #ff6b4a;
  --muted: #6b7280;
  --max-width: 1100px;
}

body {
  font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
  background: var(--bg);
  color: #111827;
  line-height: 1.5;
  display: flex;
  justify-content: center;
  padding: 0 16px 60px;
}

/* Container centering via max-width */
header, .section, footer, .hero {
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
}

/* Header (nav-bar must be inside header) */
#header {
  background: rgba(255,255,255,0.95);
  backdrop-filter: blur(4px);
  box-shadow: 0 6px 18px rgba(16,24,40,0.06);
  position: fixed; /* keeps nav always at top */
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
}

/* add top padding to body content by giving body a top margin via header height */
body { padding-top: 78px; } /* ensures content not hidden under fixed header */

/* Header inner uses flexbox */
.header-inner {
  display: flex; /* flexbox usage */
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 14px 20px;
}

/* logo image must be descendant of header and have id header-img */
.logo-wrap { display:inline-block; }
#header-img {
  height: 48px;
  width: 48px;
  object-fit: cover;
  border-radius: 8px;
}

/* Nav */
#nav-bar {
  display: flex;
  gap: 14px;
  align-items: center;
}
#nav-bar .nav-link,
#nav-bar a {
  text-decoration: none;
  color: #111827;
  padding: 8px 12px;
  border-radius: 8px;
  font-weight: 600;
  transition: background 0.18s, color 0.18s;
}
#nav-bar .nav-link:hover { background: rgba(59,130,246,0.08); color: #0f172a; }

/* Hero */
.hero {
  margin-top: 20px;
  padding: 32px 0;
}
.hero-inner {
  display: flex;
  gap: 24px;
  align-items: center;
  justify-content: space-between;
}
.hero-copy { flex: 1 1 420px; }
.hero-copy h1 { font-size: 1.8rem; margin-bottom: 8px; }
.hero-copy .tag { color: var(--muted); margin-bottom: 12px; }
.btn {
  display: inline-block;
  background: linear-gradient(180deg,var(--accent),#ff3f23);
  color: #fff;
  padding: 10px 14px;
  border-radius: 10px;
  text-decoration: none;
  font-weight: 700;
}
.hero-image { flex: 1 1 420px; text-align: right; }
.hero-image img { width: 100%; max-width: 420px; border-radius: 12px; }

/* Section general */
.section { padding: 36px 0; }
.section h2 { margin-bottom: 14px; font-size: 1.2rem; }

/* features grid (flex or grid) */
.features-grid {
  display: flex; /* flexbox usage again */
  gap: 18px;
  flex-wrap: wrap;
}
.feature {
  background: var(--card);
  border-radius: 10px;
  padding: 18px;
  flex: 1 1 260px;
  box-shadow: 0 8px 18px rgba(16,24,40,0.04);
}

/* Video */
.video-wrap {
  max-width: 900px;
  aspect-ratio: 16/9;
  background: #000;
  border-radius: 12px;
  overflow: hidden;
}
.video-wrap iframe {
  width: 100%;
  height: 100%;
  border: 0;
}

/* Pricing cards */
.pricing-cards {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}
.card {
  background: var(--card);
  padding: 18px;
  border-radius: 10px;
  flex: 1 1 220px;
  text-align: center;
  box-shadow: 0 8px 18px rgba(16,24,40,0.04);
}
.card.featured { transform: scale(1.02); border: 2px solid rgba(255,107,74,0.12); }

/* Form */
#form {
  display: flex;
  gap: 10px;
  align-items: center;
  max-width: 720px;
  flex-wrap: wrap;
}
#email {
  padding: 12px 14px;
  border-radius: 10px;
  border: 1px solid #e6e9ef;
  flex: 1 1 320px;
  font-size: 1rem;
}
#submit {
  background: linear-gradient(180deg,var(--accent),#ff3f23);
  color: #fff;
  border: none;
  padding: 12px 16px;
  border-radius: 10px;
  font-weight: 700;
  cursor: pointer;
}

/* footer */
footer.small { padding: 18px 0; text-align: center; color: var(--muted); font-size: 0.95rem; }

/* Utility: screen-reader-only */
.sr-only { position: absolute; width:1px; height:1px; padding:0; margin:-1px; overflow:hidden; clip:rect(0,0,0,0); border:0; }

/* Media query — required by tests */
@media (max-width: 880px) {
  .hero-inner { flex-direction: column-reverse; gap: 18px; text-align: center; }
  .hero-image { text-align: center; }
  #nav-bar { gap: 8px; }
  .features-grid { flex-direction: column; }
  body { padding-top: 90px; } /* allow for taller header on small screens */
}


** end of styles.css **

